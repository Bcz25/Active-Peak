// This is the postcss configuration file that is used to compile the CSS files.
module.exports = {
  // These plugins are used to compile the CSS files.
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
    },
  }